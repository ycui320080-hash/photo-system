# 验收记录（2026-09-08）

## 实际执行结果

|检查|结果|说明|
|---|---|---|
|Node/npm/Git环境|通过|Node24.14.0、npm11.9.0、Git2.54.0；PowerShell使用npm.cmd|
|npm install|完成|依赖及package-lock.json已生成|
|TypeScript / vue-tsc|通过|shared、server、两端TS/Vue和测试|
|ESLint|通过|TS/Vue业务代码；云函数和Electron另做语法检查|
|自动化测试|3项全部通过|业务流程、上传队列、图片边界|
|管理端Mock构建|通过|desktop-admin/dist|
|管理端CloudBase构建|通过|.data/cloud-build/admin；只验证编译，未连接云环境|
|小程序H5构建|通过|miniapp/dist/build/h5|
|微信小程序构建|通过|miniapp/dist/build/mp-weixin|
|Electron实际启动|通过|44.2.0，输出ELECTRON_SMOKE_OK并退出|
|云函数及Electron JS语法|通过|所有部署目录和主进程脚本逐一node --check|
|CloudBase初始化数据|已生成|docs/cloud-seed：15个套餐、规格、门店与未来30天时段；仅用于空环境|
|业务文件拆分|通过|本次检查的TS/Vue业务文件均未超过300行；页面、服务、常量和校验分离|

## 测试覆盖

workflow.test.ts在独立数据库中验证：服务端套餐计价拒绝前端金额/角色/用户ID伪造；预约幂等、时段容量、四位取件码；顾客无权访问统计或导出；状态不能跳跃且历史保留；水印上传去重；错误照片ID拒绝、选片提交锁定；收款与统计一致；成片上传、一次修改限制、确认成片、合法下载、过期下载拒绝；冲印金额700分；取消预约及备份权限。

queue.test.ts验证：SQLite任务去重、状态持久化、进程中断恢复、失败重试时间、参数化保存含引号目录及索引去重。

image.test.ts验证：295×413、5000×100、20×20图片均可生成JPG水印预览与不超过320像素宽的缩略图。测试图片均为本地生成纯色图，不含真人资料。

## 实际浏览器验证

已在浏览器中打开顾客首页、进入预约、提交演示订单CP1788845805032881；页面显示预约成功与取件码。已登录管理端并确认同一订单可见、待确认1单、待收款29元、实收0元，并查看管理端截图。

后续重启后的浏览器复查被自动审批拒绝，返回理由是本地管理页访问未获许可，未说明更具体原因。没有绕过限制。此后的代码以类型检查、构建、接口测试及Electron启动检查验证；不声称新增界面逐项经过浏览器点击验收。

## 尚未实际验收与已知边界

- 未配置真实微信AppID及CloudBase环境，因此未进行云资源部署、真实微信用户登录、真机相册/导航和云数据库/存储规则联调。已有代码和可编译适配不能等同于上线验收。
- 云函数上传目前使用base64请求，业务限制10MB；需要验证真实云套餐的函数载荷上限，可能需改用分块上传。云恢复单批最多50条。
- Mock是单店单顾客演示，默认身份固定；不应将该API开放到公网。数据统计来自已保存订单，无伪造营业额。
- 微信Mock上传每次一张，H5与管理端每批最多20张。微信开发者工具的实际导入运行未执行，仅完成目标构建。
- 密码与会话由Mock进程管理，重启后重新生成；会话有效期8小时，账号一分钟最多10次失败尝试。CloudBase生产账号由官方SDK和云控制台管理。
- 未接微信支付、不直接控制打印机、不创建安装包或代码签名；Electron以开发项目方式启动。
- 构建仍有大文件块、Uni-app循环依赖、未配置AppID统计不可用，以及Node SQLite实验特性提示，未阻断构建或测试。
- 依赖审计仍报告 47 项：低 5、中 25、高 17、严重 0。已执行npm audit fix并升级sharp至0.35.4、Electron至44.2.0；未使用会破坏Uni-app版本组合的强制降级。完整报告见dependency-audit.json，生产上线前需完成剩余依赖治理。

## 启动与保留

根目录start.cmd启动本机服务；start-desktop.cmd打开Electron。README含逐项命令、临时账号获取方式和备份恢复说明。Git已初始化但未创建提交。原始照片不会被自动移动或删除；测试与构建产物位于项目目录内。
