const {wrap,log}=require('./runtime');exports.main=wrap(async({event,openid,db})=>{let profile;try{profile=(await db.collection('users').doc(openid).get()).data}catch{}profile=profile||{id:openid,contacts:[],status:'active',createdAt:new Date().toISOString()};if(event.contacts!==undefined){if(!Array.isArray(event.contacts)||event.contacts.length>10||event.contacts.some(c=>typeof c.name!=='string'||c.name.length>40||!/^1[3-9]\d{9}$/.test(c.phone)))throw Error('联系人格式无效');profile.contacts=event.contacts.map(c=>({name:c.name,phone:c.phone}))}if(event.deletionRequested)profile.deletionRequested=true;profile.updatedAt=new Date().toISOString();const {_id,...data}=profile;await db.collection('users').doc(openid).set({data});await log(db,openid,'profile-update',openid);return data});