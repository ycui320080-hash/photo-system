const {wrap,order,writeOrder,log}=require('./runtime');exports.main=wrap(async({event,openid,staff,db})=>{const o=await order(db,event.id,openid,staff),p=o.photos.find(p=>p.id===event.photoId);if(!p?.selected)throw Error('照片未选中');await db.collection('retouch_requests').doc(p.id).set({data:{id:p.id,orderId:o.id,photoId:p.id,note:p.note||'',status:event.done?'completed':'working',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()}});await log(db,openid,'retouch-progress',p.id);return {updated:true};},'staff');