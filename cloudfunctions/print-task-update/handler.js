const {wrap,order,writeOrder,log}=require('./runtime');exports.main=wrap(async({event,openid,staff,db})=>{const o=await order(db,event.id,openid,staff);if(o.status!=='待打印')throw Error('不是待打印订单');await db.collection('print_tasks').doc(o.id).set({data:{id:o.id,orderId:o.id,status:'printing',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()}});await log(db,openid,'printing-start',o.id);return {updated:true};},'staff');